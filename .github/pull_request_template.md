## What

<!-- Brief description of the change -->

## Checklist

- [ ] Mac app builds (`xcodebuild ... -sdk macosx`)
- [ ] CLI builds (`swift build --package-path CLI/`)
- [ ] Tests pass (`xcodebuild test ...`)
- [ ] Uses `@Observable`, not `ObservableObject`
- [ ] No diffs stored in database
